services:
  agent:
    build: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro
    mem_limit: 512m
    cpus: 1.0
    restart: "no"
    command: ["scan"]
